#!/usr/bin/env bash
set -e

echo "[lint] Running lint + typecheck..."

# Adapt to your stack:
# npm run lint && npm run typecheck
# ruff check . && mypy .
# golangci-lint run

echo "[lint] No lint command configured yet — edit scripts/lint.sh."
